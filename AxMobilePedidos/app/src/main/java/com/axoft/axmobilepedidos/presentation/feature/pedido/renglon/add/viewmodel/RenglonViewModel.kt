package com.axoft.axmobilepedidos.presentation.feature.pedido.renglon.add.viewmodel

import androidx.lifecycle.ViewModel
import javax.inject.Inject

/**
 * RenglonViewModel
 *
 * ViewModel placeholder para gestionar la lógica asociada a los renglones
 * del pedido. Actualmente no contiene lógica implementada, pero está preparado
 * para recibir inyección de dependencias mediante @Inject, facilitando
 * futuras extensiones como manejo de cantidades, validaciones o cálculos
 * sobre los renglones.
 */
class RenglonViewModel @Inject constructor() : ViewModel()